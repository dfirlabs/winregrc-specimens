# Checks scripts.
name: scripts
on:
  pull_request:
  push:
permissions: read-all
jobs:
  run_scripts_windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v6
    - name: Generate test data
      run: |
        .\clean-specimens-windows.ps1
        .\export-specimens-windows.ps1
        .\generate-specimens-windows.ps1
        .\export-specimens-windows.ps1
    - uses: actions/upload-artifact@v7
      with:
        name: specimens-windows
        path: specimens/*/*
