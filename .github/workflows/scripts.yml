# Checks scripts.
name: scripts
on:
  pull_request:
  push:
permissions: read-all
jobs:
  run_scripts_windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v6
    - name: Generate test data
      run: |
        .\clear-registry.ps1
        .\show-registry.ps1
        .\generate-specimens1.ps1
        .\show-registry.ps1
        .\clear-registry.ps1
        .\generate-specimens2.ps1
        .\show-registry.ps1
    - uses: actions/upload-artifact@v7
      with:
        name: specimens-windows
        path: specimens/*/*
